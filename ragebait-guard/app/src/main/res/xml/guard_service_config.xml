<?xml version="1.0" encoding="utf-8"?>
<!--
  No android:packageNames filter on purpose: we need window-state events from
  *other* apps too, so we can hide the overlay button the moment TikTok leaves
  the foreground. TikTok filtering happens in code (ScreenReader.isTikTok).
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowContentChanged|typeWindowStateChanged|typeWindowsChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:description="@string/service_description"
    android:notificationTimeout="150"
    android:summary="@string/service_summary" />
